# Implementations
Execute these steps sequentially. Do not stop until the entire list is complete.

# Objective
To make the #ActivityPage Functional

# Phase 1: Fix skyward-journey-tooltip-anchor
- The "Start" button does not work and it wont let the user click it as it will close when you click the "Start Button"
- Fix this issues, and make sure the "Start" button works as expected.

# Phase 2: Where does the user go after clicking the "Start" button?
- After clicking the "Start" button, the user should be redirected to the next page which is the #TrainingPage.
- The tp-topic-card should be the step.objective.
- Take into account the step.objective as there are for example indicated time limit, for example 'Talk about your Favorite Breakfast for 15 Seconds'
- Use the same bar we use #UsePretestPage to show if they achieve the '15 Seconds Mark'.

# Phase 3: Back Buttons
- Add back buttons to the #TrainingPage and #PracticePage.
- The back buttons should redirect the user to the previous page.
- Make sure the back buttons work as expected.

# Phase 4: Commit and Push
- Commit the changes to the repository.
- Push the changes to the remote repository.
